#!/usr/bin/sh
vol=$(amixer get Master | awk -F'[]%[]' '/%/ {if ($7 == "off") { print "Muted" } else { print $2 }}' | head -n 1)

if [ "$vol" == "Muted" ]; then
    echo $vol
else
    echo $vol%
fi

exit 0
