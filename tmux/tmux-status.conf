# Mode
set -g mode-style bg=brightblack
set -g mode-style fg=default

# Use top tabbar
set -g status-position bottom

# Status update interval
set -g status-interval 5

# Basic status bar colors
set -g status-bg "#302d41"
set -g status-fg brightcyan

# Left side of status bar
set -g status-left-length 40
set -g status-left ''

# Window status
set -g window-status-format '#[fg=black,bg=#302d41] #I #[fg=#1a1826,bg=#575268] #W '
set -g window-status-current-format '#[fg=#d9e0ee,bg=#1e1e2e] #I #[fg=#ffffff,bg=#1a1826] #{=10:window_name} '
set -g window-status-separator ""
set -g status-justify left

# Right side of status bar
set -g status-right-length 40
set -g status-right '#[fg=#1a1826,bg=#575268] #S '

# Pane border
set -g pane-border-style bg=default
set -g pane-border-style fg=black
set -g pane-active-border-style bg=default
set -g pane-active-border-style fg=white

# Pane number indicator
set -g display-panes-colour brightblack
set -g display-panes-active-colour brightwhite

# Clock mode
set -g clock-mode-colour red
set -g clock-mode-style 24

# Message
set -g message-style bg=default
set -g message-style fg=default
