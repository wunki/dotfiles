# -*- mode: shell-script -*-
# ~/.bash_aliases
#

if [ `uname` == "Linux" ]; then
    alias ls="ls --color=auto"
fi
alias e="emacs"
alias v="vim"

if hash hub 2>/dev/null; then
    eval "$(hub alias -s)"
fi
