#!/usr/bin/env bash

# Only runs the supplied command if the network is up
ping -W5 -c1 wunki.org > /dev/null
if [[ "$?" -ne 0 ]]; then
    echo "No internet connection found."
    echo "Exited before running command: $@"
    exit 1
fi

# Else, run everything they asked for.
$@
